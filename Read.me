# FE libraries

For shoelace copy contents of cdn chunks and components

- Modify for icon path (in the chunks, search getBasePath)
- Modify for component path (in the autoload)

git tag -a v.0.1 -m "Release version 0.0.1"